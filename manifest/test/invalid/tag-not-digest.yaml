# A tag instead of a digest. Rejected: a run that cannot say which bytes it ran
# is a run whose result stops meaning anything tomorrow.
schema: 1
project: tagged
guests: [some-guest]
exec: container
build:
  image: docker.io/library/example:latest
  cmd: make build
run:
  cmd: make e2e

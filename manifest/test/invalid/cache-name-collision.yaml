# Distinct as names, identical as environment variables: both become
# REAPER_CACHE_MY_CACHE after mangling, and only one is reachable that way.
schema: 1
project: mangled
guests: [some-guest]
exec: host
build:
  cmd: make deps
  cache: [my-cache, my.cache]
run:
  cmd: make check
